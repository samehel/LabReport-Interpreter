DATABASE_URL=sqlite+aiosqlite:///./lab_reports.db
SECRET_KEY=
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=10
CORS_ORIGINS=*

# Email / SMTP (set SMTP_ENABLED=true once configured)
SMTP_ENABLED=true
SMTP_HOST=mail.spacemail.com
SMTP_PORT=465
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true
SMTP_START_TLS=false
EMAIL_FROM_ADDRESS=
EMAIL_FROM_NAME=LabReport Interpreter
OTP_EXPIRE_MINUTES=10
BASE_URL=http://localhost:8000
